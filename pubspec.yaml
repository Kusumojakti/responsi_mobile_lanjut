name: responsi_mobile_lanjut
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  get: 4.6.6
  flutter_otp_text_field: ^1.2.0
  just_audio: ^0.9.42
  audio_video_progress_bar: ^2.0.3
  rxdart: ^0.27.0
  cupertino_icons: ^1.0.8
  persistent_bottom_nav_bar: ^6.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/
  - assets/images/

